import { Box } from '@mui/material';

export const Users = () => {
    return (
        <Box>
            <p>이 페이지는 자동 생성된 라우트로 접근됩니다!</p>
            <p>페이지 설정 추가(<code>constants/app-config.ts</code>)하고 컴포넌트만 생성하면 라우트가 자동으로 생성됩니다.</p>
        </Box>
    );
};
