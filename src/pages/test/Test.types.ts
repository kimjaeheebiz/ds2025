// Generated types for Test
// Note: Basic data types (User, etc.) are defined in @/data

// Page-specific types for Test
export interface TestPageState {
    isLoading: boolean;
    error: string | null;
    // Add page-specific state types here
}

export type BoxProps = object;

export type TestProps = object;
