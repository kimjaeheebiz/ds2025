import React from 'react';
import { Box, Typography } from '@mui/material';

export const SettingsContent: React.FC = () => {
    return (
        <Box>
            <Typography variant="body2">프로젝트 설정</Typography>
        </Box>
    );
};
